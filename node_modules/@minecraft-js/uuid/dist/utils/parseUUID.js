"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.parseUUID = void 0;
const UUID_1 = require("../UUID");
const typeAssertion_1 = require("./typeAssertion");
/**
 * Parses the given UUID
 * @param uuid Raw UUID to parse
 * @param disableTypeCheck Whether or not to disable type checking
 * @returns The `UUID` instance corresponding to the given UUID
 */
function parseUUID(uuid, disableTypeCheck) {
    if (disableTypeCheck !== true &&
        !((0, typeAssertion_1.isStringUUID)(uuid) || (0, typeAssertion_1.isStringUUID)(uuid, false)))
        throw new TypeError('The uuid argument should be a valid uuid (with dashes or not) with a type of string');
    uuid = uuid.replace(/-/g, '');
    const mostSigBits = Buffer.from(uuid.slice(0, 16), 'hex');
    const leastSigBits = Buffer.from(uuid.slice(16, 32), 'hex');
    return new UUID_1.UUID(mostSigBits, leastSigBits);
}
exports.parseUUID = parseUUID;
